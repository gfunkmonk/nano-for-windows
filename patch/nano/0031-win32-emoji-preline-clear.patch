--- a/src/winio.c	2026-05-20 10:15:00.000000000 -0400
+++ b/src/winio.c	2026-05-20 10:20:00.000000000 -0400
@@ -2649,6 +2649,18 @@
  */
 void draw_row(int row, const char *converted, linestruct *line, size_t from_col)
 {
+#ifdef _WIN32
+	/* Pre-clear margin to prevent emoji artifacts when paging. */
+	if (margin > 0)
+		mvwprintw(midwin, row, 0, "%*s", margin, "");
+#endif
+
 #ifdef ENABLE_LINENUMBERS
 	/* If line numbering is switched on, put a line number in front of
 	 * the text -- but only for the parts that are not softwrapped. */
