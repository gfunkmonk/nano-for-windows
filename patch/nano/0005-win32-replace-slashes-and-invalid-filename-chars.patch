From fe644ef6b46274ef00cf9f58e46c356d55c0e627 Mon Sep 17 00:00:00 2001
From: Chawye Hsu <su+git@chawyehsu.com>
Date: Sat, 18 Apr 2026 13:12:46 +0800
Subject: [PATCH 05/16] win32: replace slashes and invalid filename chars

Signed-off-by: Chawye Hsu <su+git@chawyehsu.com>
---
 src/files.c      |  7 ++++++-
 src/history.c    |  1 +
 src/prototypes.h |  1 +
 src/utils.c      | 13 +++++++++++++
 4 files changed, 21 insertions(+), 1 deletion(-)

diff --git a/src/files.c b/src/files.c
index f93acc0..36f8c3a 100644
--- a/src/files.c
+++ b/src/files.c
@@ -1435,6 +1435,8 @@ char *get_full_path(const char *origpath)
 
 	free(untilded);
 
+	normalize_slashes(target);
+
 	return target;
 }
 
@@ -1477,6 +1479,7 @@ char *safe_tempfile(FILE **stream)
 		tempdir = copy_of("/tmp/");
 
 	extension = strrchr(openfile->filename, '.');
+	normalize_slashes(extension);
 
 	if (!extension || strchr(extension, '/'))
 		extension = openfile->filename + strlen(openfile->filename);
@@ -1627,7 +1630,7 @@ bool make_backup_of(char *realname)
 		 * just use the file-name portion of the given path. */
 		if (thename) {
 			for (int i = 0; thename[i] != '\0'; i++)
-				if (thename[i] == '/')
+				if (strchr("<>:\"/\\|?*", thename[i]))
 					thename[i] = '!';
 		} else
 			thename = copy_of(tail(realname));
@@ -2425,6 +2428,8 @@ char *real_dir_from_tilde(const char *path)
 
 	free(tilded);
 
+	normalize_slashes(retval);
+
 	return retval;
 }
 
diff --git a/src/history.c b/src/history.c
index b1f245c..cf5451d 100644
--- a/src/history.c
+++ b/src/history.c
@@ -412,6 +412,7 @@ void load_positions_register(void)
 
 	/* Read and parse each line, and store the extracted data. */
 	while (count++ < 200 && (length = getline(&phrase, &dummy, registry)) > 1) {
+		normalize_slashes(phrase);
 		stanza = strchr(phrase, '/');
 		length -= (stanza ? stanza - phrase : 0);
 
diff --git a/src/prototypes.h b/src/prototypes.h
index 488a686..8553779 100644
--- a/src/prototypes.h
+++ b/src/prototypes.h
@@ -558,6 +558,7 @@ void complete_a_word(void);
 /* All functions in utils.c. */
 void get_homedir(void);
 const char *tail(const char *path);
+void normalize_slashes(char *path);
 char *concatenate(const char *path, const char *name);
 int digits(ssize_t n);
 bool parse_num(const char *str, ssize_t *result);
diff --git a/src/utils.c b/src/utils.c
index f061b5a..84df35c 100644
--- a/src/utils.c
+++ b/src/utils.c
@@ -62,6 +62,17 @@ const char *tail(const char *path)
 		return slash + 1;
 }
 
+/* Replace any backslashes with slashes in the given path. */
+void normalize_slashes(char *path)
+{
+	if (path == NULL)
+		return;
+
+	for (; *path; ++path)
+		if (*path == '\\')
+			*path = '/';
+}
+
 /* Return a copy of the two given strings, welded together. */
 char *concatenate(const char *path, const char *name)
 {
@@ -71,6 +82,8 @@ char *concatenate(const char *path, const char *name)
 	strcpy(joined, path);
 	strcpy(joined + pathlen, name);
 
+	normalize_slashes(joined);
+
 	return joined;
 }
 
-- 
2.53.0.windows.3

