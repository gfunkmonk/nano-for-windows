--- a/curses/pdcurses/getch.c
+++ b/curses/pdcurses/getch.c
@@ -105,6 +105,10 @@

 static int PDC_function_key[PDC_MAX_FUNCTION_KEYS] = { 0, 22, 0, 0, 0, 0, 3 };

+#ifdef PDC_FORCE_UTF8
+extern int PDC_wc_to_utf8(char *dest, const int32_t code);
+#endif
+
 /*man-start**************************************************************

 Function keys
@@ -797,6 +797,23 @@ int wgetch(WINDOW *win)
         rval = _raw_wgetch(win);
         if( rval != ERR && (rval < 0 || rval > 127)
                   && !PDC_is_function_key( rval))
+#ifdef PDC_FORCE_UTF8
+        {
+            /* Encode the Unicode code point directly as UTF-8. */
+            int n_bytes = PDC_wc_to_utf8( (char *)buffered, rval);
+
+            if( n_bytes <= 0)
+            {
+                n_buff = 0;
+                rval = ERR;
+            }
+            else
+            {
+                n_buff = (size_t)n_bytes;
+                rval = wgetch( win);
+            }
+        }
+#else
         {
             wchar_t c[2];

@@ -811,6 +834,7 @@ int wgetch(WINDOW *win)
             else     /* successfully converted to multi-byte string */
                 rval = wgetch( win);
         }
+#endif
     }
     return( rval);
 #endif
