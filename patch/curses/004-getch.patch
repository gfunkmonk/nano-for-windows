--- a/curses/pdcurses/getch.c	2026-05-01 23:28:01.126777036 -0400
+++ b/curses/pdcurses/getch.c	2026-05-02 01:27:00.499151455 -0400
@@ -3,6 +3,8 @@
 #include <curspriv.h>
 #include <assert.h>
 
+static int _raw_wgetch(WINDOW *win);
+
 /*man-start**************************************************************
 
 getch
@@ -687,22 +689,6 @@
     }
 }
 
-static int _raw_wgetch( WINDOW *win)
-{
-   int rval = _raw_wgetch_no_surrogate_pairs( win);
-
-#ifdef PDC_WIDE
-   if( PDC_IS_HIGH_SURROGATE( rval))
-      {
-      const int c = _raw_wgetch_no_surrogate_pairs( win);
-
-      if( PDC_IS_LOW_SURROGATE( c))
-         rval = ((rval - 0xd800) << 10) + 0x10000 + c - 0xdc00;
-      }
-#endif
-   return( rval);
-}
-
 int mvgetch(int y, int x)
 {
     PDC_LOG(("mvgetch() - called\n"));
@@ -809,13 +795,58 @@
                 rval = ERR;
             }
             else     /* successfully converted to multi-byte string */
-                rval = wgetch( win);
+                rval = wgetch(win);
         }
     }
     return( rval);
 #endif
 }
 
+static int _raw_wgetch(WINDOW *win)
+{
+    /* 
+     * Step 1: Call your existing massive function.
+     * All your original logic and comments are safe inside here.
+     */
+    int ch = _raw_wgetch_no_surrogate_pairs(win);
+
+#ifdef PDC_WIDE
+    /* 
+     * Step 2: Check if the character is the start of an emoji (High Surrogate).
+     */
+    if (ch >= 0xD800 && ch <= 0xDBFF)
+    {
+        /* Grab the second half of the pair */
+        int low = _raw_wgetch_no_surrogate_pairs(win);
+        
+        if (low >= 0xDC00 && low <= 0xDFFF)
+        {
+            /* Combine them into a single 32-bit Unicode point */
+            ch = ((ch - 0xD800) << 10) + (low - 0xDC00) + 0x10000;
+            /* Convert the SMP codepoint to a 4-byte UTF-8 sequence.
+             * Unget the three trailing continuation bytes so that
+             * subsequent wgetch() calls deliver them in order.  Only
+             * the leading byte is returned here.  This matches the
+             * PDC_display_utf8=TRUE convention used throughout the
+             * build and lets nano's byte-oriented UTF-8 input path
+             * process pasted supplementary-plane emoji correctly. */
+            PDC_ungetch(0x80 | (ch & 0x3F));
+            PDC_ungetch(0x80 | ((ch >> 6) & 0x3F));
+            PDC_ungetch(0x80 | ((ch >> 12) & 0x3F));
+            ch = 0xF0 | ((ch >> 18) & 0x07);
+        }
+        else
+        {
+            /* If the second part is invalid, just return it as-is */
+            PDC_ungetch(low);
+            return ch; 
+        }
+    }
+#endif
+
+    return ch;
+}
+
 #ifdef PDC_WIDE
 int wget_wch(WINDOW *win, wint_t *wch)
 {
