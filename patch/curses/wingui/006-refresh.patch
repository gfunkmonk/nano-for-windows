--- a/curses/pdcurses/refresh.c
+++ b/curses/pdcurses/refresh.c
@@ -158,11 +158,21 @@
 void PDC_transform_line_sliced( int lineno, int x, int len, const chtype *srcp)
 {
-    assert( x >= 0);
-    assert( len > 0);
-    assert( x + len <= COLS);
-    assert( lineno >= 0);
-    assert( lineno < SP->lines);
+    /* Defensive bounds checks instead of hard asserts — these conditions
+     * can occur transiently during window resize or font changes and must
+     * not abort the process. */
+    if( x < 0 || len <= 0 || lineno < 0 || !SP || lineno >= SP->lines)
+        return;
+    if( x >= COLS)
+        return;
+    if( x + len > COLS)
+        len = COLS - x;
     if( lineno == SP->cursrow && x <= SP->curscol && x + len > SP->curscol)
@@ -184,11 +194,28 @@
         }
     }
 #ifdef PDC_WIDE
-    if( x && (*srcp & A_CHARTEXT) == DUMMY_CHAR_NEXT_TO_FULLWIDTH)
-    {                   /* starting on a dummy next to a fullwidth */
-        x--;
-        srcp--;
-        len++;
+    if( (*srcp & A_CHARTEXT) == DUMMY_CHAR_NEXT_TO_FULLWIDTH)
+    {
+        if( x)
+        {               /* starting on a dummy next to a fullwidth */
+            x--;
+            srcp--;
+            len++;
+        }
+        else
+        {               /* orphaned dummy at line start: skip it safely */
+            srcp++;
+            len--;
+            if( !len)
+                return;
+        }
+    }
+    /* Defensive check for MAX_UNICODE at end of buffer (resize safety) */
+    if( len > 0 && (srcp[len - 1] & A_CHARTEXT) == MAX_UNICODE)
+    {
+        len--;
+        if( !len)
+            return;
     }
 #endif
     while( len)
@@ -202,8 +219,14 @@
                      && i < len)
            i++;
         if( i == 1 && ch == MAX_UNICODE)
-            fprintf( stderr, "line %d, x=%d, len=%d\n", lineno, x, len);
-        assert( i > 1 || ch != MAX_UNICODE);
+        {      /* stray fullwidth-dummy in packet; ignore instead of aborting */
+            srcp++;
+            x++;
+            len--;
+            if( !len)
+                return;
+            continue;
+        }
         PDC_transform_line( lineno, x,
                           i - ((ch == MAX_UNICODE) ? 1 : 0), srcp);
 #else
